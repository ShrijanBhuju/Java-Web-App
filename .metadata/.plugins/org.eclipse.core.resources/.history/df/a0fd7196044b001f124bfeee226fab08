@charset "ISO-8859-1";
*{
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Poppins', sans-serif;
    filter: blur(0);
    transition: filter 0.3s ease;
}
/* Container for the sign-up form */
/* Container for the sign-up form */
/* General styles for the sign-up form container */

/* General styles for the sign-up form container */
.sign-up {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    background-color: #f0f0f0;
    font-family: Arial, sans-serif;
}

/* Styling for the form */
.sign-up form {
    background-color: #ffffff;
    padding: 20px;
    border-radius: 8px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    width: 100%;
    max-width: 500px;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* Styling for the input field container */
.input-field {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15px;
    width: 100%;
}

/* Styling for the heading */
.input-field h1 {
    color: #333;
    text-align: center;
}

/* Styling for the image field */
.image-field {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10px;
}

/* Styling for the image */
.image-field img {
    border-radius: 8px;
}

/* Styling for the file input */
.imageChooser {
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 14px;
    color: #333;
}

/* Styling for the fields container */
.fields {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15px;
    width: 100%;
}

/* Styling for each span containing a label and input */
.fields span {
    display: flex;
    flex-direction: column;
    width: 100%;
}

/* Styling for the labels */
.fields label {
    font-weight: bold;
    margin-bottom: 5px;
}

/* Styling for the input fields */
.fields input[type="text"],
.fields input[type="email"],
.fields input[type="password"],
.imageChooser {
    width: 100%;
    padding: 10px;
    border: 1px solid #ccc;
    border-radius: 4px;
    font-size: 14px;
    color: #333;
}

/* Styling for the messages */
.message {
    color: red;
    text-align: center;
    margin-top: 10px;
}

/* Styling for the submit button */
input[type="submit"] {
    background-color: #04ba80;
    color: white;
    padding: 10px 20px;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    font-size: 14px;
    transition: background-color 0.3s;
    width: 100%;
    max-width: 200px;
    margin-top: 10px;
    text-align: center;
}

/* Hover effect for the submit button */
input[type="submit"]:hover {
    background-color: #028a62;
}

/* Styling for the login button */
.login-btn {
    display: inline-block;
    color: #04ba80;
    text-decoration: none;
    padding: 10px 20px;
    border-radius: 4px;
    border: 1px solid #04ba80;
    transition: background-color 0.3s, color 0.3s;
    text-align: center;
    width: 100%;
    max-width: 200px;
    margin-top: 10px;
}

/* Hover effect for the login button */
.login-btn:hover {
    background-color: #04ba80;
    color: white;
}

